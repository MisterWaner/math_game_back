export type User = {
    id?: number,
    username: string,
    password: string,
    age: number,
    level: string,
    isRegistered: boolean,
    dailyScore: number,
    weeklyScore: number,
    monthlyScore: number,
    globalScore: number,
}